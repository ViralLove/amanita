Licensing Overview

This repository uses multiple licenses:

* /contracts, /bot, /scripts → Apache 2.0
* /wallet → Restricted (BSL-style license)
* /wallet/brand → All rights reserved

Key principle:

The protocol is open.
The network is shared.
The brand and reference client are protected.

If a directory contains its own LICENSE file, it overrides the root license.
